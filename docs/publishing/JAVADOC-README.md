# AdvancedBiometricPromptCompat documentation

This documentation JAR contains generated Dokka Java/Kotlin API reference. Open `index.html`.

- Project documentation and integration examples:
  https://github.com/Salat-Cx65/AdvancedBiometricPromptCompat
- Module source and documentation:
  https://github.com/Salat-Cx65/AdvancedBiometricPromptCompat/tree/main
- For the exact source corresponding to this artifact version, use its companion `sources.jar`.
- A module-specific README is included under `module/` when one exists.
- Existing module third-party notices are included under `META-INF/` when present. This does not
  replace the licenses or documentation distributed with independently supplied vendor SDKs/models.

Optional SDK runtimes and models are supplied separately by the consuming application. Consult
the module README for initialization, dependency and runtime requirements.
